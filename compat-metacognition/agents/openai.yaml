interface:
  display_name: "Compat Metacognition"
  short_description: "Load only when the procedure is not known"
  default_prompt: "Use $compat-metacognition to determine whether this change must preserve existing state."
