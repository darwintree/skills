interface:
  display_name: "Compat Metacognition"
  short_description: "Check stability before changing anything that exists"
  default_prompt: "Use $compat-metacognition to check stability before keeping compatibility."
